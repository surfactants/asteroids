#pragma once
#include <SFML/Graphics.hpp>

//REQUIREMENTS:
    //one drop-down menu for type selection
    //two textboxes (subject, message)
    //two buttons: submit, cancel
        //submit button should only be available if both textboxes have text
        //escape should cancel as well

        //submitting uses sf::TcpSocket, see feedback project for more
class Feedback : public sf::Drawable{
public:
protected:
private:
};
